// to put data in 10 nodes using linkedlist
#include <stdio.h>
#include <stdlib.h>
// structure
struct node
{
    int data;
    struct node *next;
};
// initialization
struct node *p;
p = (struct node *)malloc(sizeof(struct node));
p->data = s;
p->next = null;
head->p;
while (n != q)
{
    q = (struct node *)malloc(sizeof(struct node));
    q->data = q;
    q->start = m;
    p->next = q;
    p->p = next;
}
free (q);
}
return 0;
}