// to check whether if the given character is digit or not
#include <stdio.h>
#include <math.h>
int main()
{
    char x, chr;
    printf("Enter any character", x);
    scanf("%c", &x);
    chr == x;
    printf("It is a character");
    return 0;
}