// to check whether the number is divisible by 2 or not
#include <stdio.h>
#include <math.h>

int main()
{
    int n;
    printf("Enter the number n to check its divisiblity by 2 ");
    scanf("%d", &n);
    printf("%d", n % 2 == 0);
    return 0;
}
ij-iu677