// input the string and reverse the stringwith the use of pointer
#include <stdio.h>
int main()
{
    char a[100], *ptr = a;
    printf("Enter a string", *ptr);
    scanf("%c", &*ptr);
    return 0;
}